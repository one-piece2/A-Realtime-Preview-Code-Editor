# 🔐 前端登录注册方案设计文档

## 📋 目录

1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [项目结构设计](#项目结构设计)
4. [状态管理方案](#状态管理方案)
5. [路由设计](#路由设计)
6. [API 服务层设计](#api-服务层设计)
7. [类型定义](#类型定义)
8. [组件设计](#组件设计)
9. [表单验证方案](#表单验证方案)
10. [Token 管理方案](#token-管理方案)
11. [错误处理方案](#错误处理方案)
12. [保护路由方案](#保护路由方案)
13. [详细实现步骤](#详细实现步骤)
14. [测试方案](#测试方案)

---

## 项目概述

### 目标
实现完整的用户认证系统，包括：
- 本地注册（邮箱+用户名+密码）
- 本地登录（邮箱+密码）
- GitHub OAuth 登录
- Token 管理和自动刷新
- 路由保护
- 用户状态管理

### 设计原则
- **类型安全**：全面使用 TypeScript
- **组件化**：可复用的 UI 组件
- **状态集中管理**：使用 Context API
- **错误处理**：统一的错误处理机制
- **用户体验**：友好的表单验证和错误提示
- **安全性**：Token 安全存储和使用

---

## 技术栈

### 已使用技术
- **React 19** - UI 框架
- **TypeScript** - 类型系统
- **React Router v7** - 路由管理
- **Tailwind CSS** - 样式方案
- **shadcn/ui** - UI 组件库
- **Axios** - HTTP 客户端
- **Sonner** - Toast 通知
- **Context API** - 状态管理

### 需要安装的依赖
```bash
# 表单验证库（可选，也可以自己实现）
npm install react-hook-form zod @hookform/resolvers

# 或者使用简单的验证方案（推荐，更轻量）
# 不需要额外安装，使用原生验证
```

---

## 项目结构设计

### 目录结构

```
client/src/
├── api/                          # API 服务层
│   ├── auth.ts                  # 认证相关 API
│   ├── axios.ts                 # Axios 实例配置
│   └── types.ts                 # API 响应类型
│
├── auth/                         # 认证相关
│   ├── components/              # 认证组件
│   │   ├── LoginForm.tsx       # 登录表单
│   │   ├── RegisterForm.tsx    # 注册表单
│   │   ├── AuthLayout.tsx      # 认证页面布局
│   │   └── GitHubButton.tsx    # GitHub 登录按钮
│   │
│   ├── hooks/                   # 认证相关 Hooks
│   │   ├── useAuth.ts          # 认证状态 Hook
│   │   ├── useLogin.ts         # 登录逻辑 Hook
│   │   └── useRegister.ts      # 注册逻辑 Hook
│   │
│   └── utils/                   # 认证工具函数
│       ├── token.ts            # Token 管理
│       └── validation.ts       # 表单验证规则
│
├── context/                      # Context 提供者
│   ├── AuthContext.tsx         # 认证状态 Context
│   └── AuthProvider.tsx        # 认证状态提供者
│
├── pages/                        # 页面组件
│   ├── Login.tsx               # 登录页面
│   ├── Register.tsx            # 注册页面
│   ├── AuthCallback.tsx        # GitHub 回调页面
│   └── ...                     # 其他页面
│
├── components/                   # 通用组件
│   └── ui/                     # UI 组件（已存在）
│       ├── input.tsx
│       ├── button.tsx
│       └── ...
│
├── types/                        # 类型定义
│   ├── auth.ts                 # 认证相关类型
│   └── api.ts                  # API 相关类型
│
└── utils/                        # 工具函数
    └── error.ts                 # 错误处理工具
```

---

## 状态管理方案

### AuthContext 设计

**文件**: `src/context/AuthContext.tsx`

```typescript
import { createContext } from 'react';

export interface User {
  id: string;
  email: string;
  username: string;
  githubNickname?: string | null;
  githubAvatar?: string | null;
}

export interface AuthState {
  user: User | null;
  token: string | null;
  isAuthenticated: boolean;
  isLoading: boolean;
}

export interface AuthContextType extends AuthState {
  login: (email: string, password: string) => Promise<void>;
  register: (email: string, username: string, password: string) => Promise<void>;
  logout: () => void;
  setUser: (user: User | null) => void;
  setToken: (token: string | null) => void;
}

export const AuthContext = createContext<AuthContextType | undefined>(undefined);
```

### AuthProvider 设计

**文件**: `src/context/AuthProvider.tsx`

```typescript
import { useState, useEffect, ReactNode } from 'react';
import { AuthContext, User, AuthState } from './AuthContext';
import { getToken, setToken, removeToken, getUser, setUser, removeUser } from '@/auth/utils/token';

export function AuthProvider({ children }: { children: ReactNode }) {
  const [state, setState] = useState<AuthState>({
    user: null,
    token: null,
    isAuthenticated: false,
    isLoading: true,
  });

  // 初始化：从 localStorage 恢复状态
  useEffect(() => {
    const token = getToken();
    const user = getUser();
    
    if (token && user) {
      setState({
        user,
        token,
        isAuthenticated: true,
        isLoading: false,
      });
    } else {
      setState(prev => ({ ...prev, isLoading: false }));
    }
  }, []);

  const login = async (email: string, password: string) => {
    // 实现登录逻辑
  };

  const register = async (email: string, username: string, password: string) => {
    // 实现注册逻辑
  };

  const logout = () => {
    removeToken();
    removeUser();
    setState({
      user: null,
      token: null,
      isAuthenticated: false,
      isLoading: false,
    });
  };

  const setUserState = (user: User | null) => {
    if (user) {
      
      setUser(user);
    } else {
      removeUser();
    }
    setState(prev => ({ ...prev, user }));
  };

  const setTokenState = (token: string | null) => {
    if (token) {
      setToken(token);
    } else {
      removeToken();
    }
    setState(prev => ({ ...prev, token, isAuthenticated: !!token }));
  };

  return (
    <AuthContext.Provider
      value={{
        ...state,
        login,
        register,
        logout,
        setUser: setUserState,
        setToken: setTokenState,
      }}
    >
      {children}
    </AuthContext.Provider>
  );
}
```

### useAuth Hook

**文件**: `src/auth/hooks/useAuth.ts`

```typescript
import { useContext } from 'react';
import { AuthContext } from '@/context/AuthContext';

export function useAuth() {
  const context = useContext(AuthContext);
  
  if (context === undefined) {
    throw new Error('useAuth must be used within an AuthProvider');
  }
  
  return context;
}
```

---

## 路由设计

### 路由配置

**文件**: `src/App.tsx` (更新)

```typescript
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import { AuthProvider } from '@/context/AuthProvider';
import { ProtectedRoute } from '@/components/ProtectedRoute';
import { PublicRoute } from '@/components/PublicRoute';

// 页面组件
import Login from '@/pages/Login';
import Register from '@/pages/Register';
import AuthCallback from '@/pages/AuthCallback';
import Dashboard from '@/pages/Dashboard';
import Home from '@/pages/home';
// ... 其他页面

function App() {
  return (
    <AuthProvider>
      <BrowserRouter>
        <Routes>
          {/* 公开路由（未登录可访问） */}
          <Route element={<PublicRoute />}>
            <Route path="/login" element={<Login />} />
            <Route path="/register" element={<Register />} />
            <Route path="/auth/callback" element={<AuthCallback />} />
          </Route>

          {/* 受保护路由（需要登录） */}
          <Route element={<ProtectedRoute />}>
            <Route path="/" element={<Dashboard />} />
            <Route path="/home" element={<Home />} />
            <Route path="/editor/:roomId" element={<EditorSigelPage />} />
            <Route path="/editor/files" element={<EditorFilesPage />} />
            {/* ... 其他受保护的路由 */}
          </Route>

          {/* 404 */}
          <Route path="*" element={<Navigate to="/" replace />} />
        </Routes>
      </BrowserRouter>
    </AuthProvider>
  );
}
```

### ProtectedRoute 组件

**文件**: `src/components/ProtectedRoute.tsx`

```typescript
import { Navigate, Outlet, useLocation } from 'react-router-dom';
import { useAuth } from '@/auth/hooks/useAuth';

export function ProtectedRoute() {
  const { isAuthenticated, isLoading } = useAuth();
  const location = useLocation();

  if (isLoading) {
    return (
      <div className="flex items-center justify-center min-h-screen">
        <div className="text-lg">加载中...</div>
      </div>
    );
  }

  if (!isAuthenticated) {
    // 保存当前路径，登录后重定向回来
    return <Navigate to="/login" state={{ from: location }} replace />;
  }

  return <Outlet />;
}
```

### PublicRoute 组件

**文件**: `src/components/PublicRoute.tsx`

```typescript
import { Navigate, Outlet } from 'react-router-dom';
import { useAuth } from '@/auth/hooks/useAuth';

export function PublicRoute() {
  const { isAuthenticated, isLoading } = useAuth();

  if (isLoading) {
    return (
      <div className="flex items-center justify-center min-h-screen">
        <div className="text-lg">加载中...</div>
      </div>
    );
  }

  // 如果已登录，重定向到首页
  if (isAuthenticated) {
    return <Navigate to="/" replace />;
  }

  return <Outlet />;
}
```

---

## API 服务层设计

### Axios 实例配置

**文件**: `src/api/axios.ts`

```typescript
import axios, { AxiosError, InternalAxiosRequestConfig } from 'axios';
import { getToken, removeToken } from '@/auth/utils/token';

const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:3000';

export const api = axios.create({
  baseURL: API_BASE_URL,
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// 请求拦截器：自动添加 Token
api.interceptors.request.use(
  (config: InternalAxiosRequestConfig) => {
    const token = getToken();
    if (token && config.headers) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);

// 响应拦截器：处理 Token 过期
api.interceptors.response.use(
  (response) => response,
  (error: AxiosError) => {
    if (error.response?.status === 401) {
      // Token 过期或无效
      removeToken();
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

### 认证 API

**文件**: `src/api/auth.ts`

```typescript
import { api } from './axios';
import type { LoginRequest, RegisterRequest, AuthResponse } from './types';

/**
 * 本地登录
 */
export async function login(data: LoginRequest): Promise<AuthResponse> {
  const response = await api.post<AuthResponse>('/auth/login/local', data);
  return response.data;
}

/**
 * 本地注册
 */
export async function register(data: RegisterRequest): Promise<AuthResponse> {
  const response = await api.post<AuthResponse>('/auth/register/local', data);
  return response.data;
}

/**
 * GitHub 登录（重定向）
 */
export function loginWithGitHub() {
  window.location.href = `${import.meta.env.VITE_API_BASE_URL || 'http://localhost:3000'}/auth/github`;
}
```

### API 类型定义

**文件**: `src/api/types.ts`

```typescript
export interface LoginRequest {
  email: string;
  password: string;
}

export interface RegisterRequest {
  email: string;
  username: string;
  password: string;
}

export interface User {
  id: string;
  email: string;
  username: string;
  githubNickname?: string | null;
  githubAvatar?: string | null;
}

export interface AuthResponse {
  accessToken: string;
  user: User;
}
```

---

## 类型定义

### 认证相关类型

**文件**: `src/types/auth.ts`

```typescript
export interface User {
  id: string;
  email: string;
  username: string;
  githubNickname?: string | null;
  githubAvatar?: string | null;
}

export interface AuthState {
  user: User | null;
  token: string | null;
  isAuthenticated: boolean;
  isLoading: boolean;
}

export interface LoginFormData {
  email: string;
  password: string;
}

export interface RegisterFormData {
  email: string;
  username: string;
  password: string;
  confirmPassword?: string;
}
```

---

## 组件设计

### LoginForm 组件

**文件**: `src/auth/components/LoginForm.tsx`

```typescript
import { useState, FormEvent } from 'react';
import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';
import { useAuth } from '@/auth/hooks/useAuth';
import { validateLoginForm } from '@/auth/utils/validation';
import { toast } from 'sonner';

export function LoginForm() {
  const { login } = useAuth();
  const [formData, setFormData] = useState({
    email: '',
    password: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [isLoading, setIsLoading] = useState(false);

  const handleSubmit = async (e: FormEvent) => {
    e.preventDefault();
    
    // 验证表单
    const validationErrors = validateLoginForm(formData);
    if (Object.keys(validationErrors).length > 0) {
      setErrors(validationErrors);
      return;
    }

    setIsLoading(true);
    setErrors({});

    try {
      await login(formData.email, formData.password);
      toast.success('登录成功');
    } catch (error: any) {
      const message = error.response?.data?.message || '登录失败，请稍后重试';
      toast.error(message);
      if (error.response?.status === 401) {
        setErrors({ password: '邮箱或密码错误' });
      }
    } finally {
      setIsLoading(false);
    }
  };

  const handleChange = (field: string, value: string) => {
    setFormData(prev => ({ ...prev, [field]: value }));
    // 清除该字段的错误
    if (errors[field]) {
      setErrors(prev => {
        const newErrors = { ...prev };
        delete newErrors[field];
        return newErrors;
      });
    }
  };

  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      <div>
        <label htmlFor="email" className="block text-sm font-medium mb-1">
          邮箱
        </label>
        <Input
          id="email"
          type="email"
          value={formData.email}
          onChange={(e) => handleChange('email', e.target.value)}
          placeholder="请输入邮箱"
          className={errors.email ? 'border-red-500' : ''}
        />
        {errors.email && (
          <p className="text-sm text-red-500 mt-1">{errors.email}</p>
        )}
      </div>

      <div>
        <label htmlFor="password" className="block text-sm font-medium mb-1">
          密码
        </label>
        <Input
          id="password"
          type="password"
          value={formData.password}
          onChange={(e) => handleChange('password', e.target.value)}
          placeholder="请输入密码"
          className={errors.password ? 'border-red-500' : ''}
        />
        {errors.password && (
          <p className="text-sm text-red-500 mt-1">{errors.password}</p>
        )}
      </div>

      <Button type="submit" className="w-full" disabled={isLoading}>
        {isLoading ? '登录中...' : '登录'}
      </Button>
    </form>
  );
}
```

### RegisterForm 组件

**文件**: `src/auth/components/RegisterForm.tsx`

```typescript
import { useState, FormEvent } from 'react';
import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';
import { useAuth } from '@/auth/hooks/useAuth';
import { validateRegisterForm } from '@/auth/utils/validation';
import { toast } from 'sonner';

export function RegisterForm() {
  const { register } = useAuth();
  const [formData, setFormData] = useState({
    email: '',
    username: '',
    password: '',
    confirmPassword: '',
  });
  const [errors, setErrors] = useState<Record<string, string>>({});
  const [isLoading, setIsLoading] = useState(false);

  const handleSubmit = async (e: FormEvent) => {
    e.preventDefault();
    
    // 验证表单
    const validationErrors = validateRegisterForm(formData);
    if (Object.keys(validationErrors).length > 0) {
      setErrors(validationErrors);
      return;
    }

    setIsLoading(true);
    setErrors({});

    try {
      await register(formData.email, formData.username, formData.password);
      toast.success('注册成功');
    } catch (error: any) {
      const message = error.response?.data?.message || '注册失败，请稍后重试';
      toast.error(message);
      
      if (error.response?.status === 409) {
        setErrors({ email: '该邮箱已被注册' });
      } else if (error.response?.status === 400) {
        // 处理验证错误
        const errorMessages = error.response.data.message;
        if (Array.isArray(errorMessages)) {
          const newErrors: Record<string, string> = {};
          errorMessages.forEach((msg: string) => {
            if (msg.includes('邮箱')) newErrors.email = msg;
            if (msg.includes('用户名')) newErrors.username = msg;
            if (msg.includes('密码')) newErrors.password = msg;
          });
          setErrors(newErrors);
        }
      }
    } finally {
      setIsLoading(false);
    }
  };

  const handleChange = (field: string, value: string) => {
    setFormData(prev => ({ ...prev, [field]: value }));
    // 清除该字段的错误
    if (errors[field]) {
      setErrors(prev => {
        const newErrors = { ...prev };
        delete newErrors[field];
        return newErrors;
      });
    }
  };

  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      <div>
        <label htmlFor="email" className="block text-sm font-medium mb-1">
          邮箱
        </label>
        <Input
          id="email"
          type="email"
          value={formData.email}
          onChange={(e) => handleChange('email', e.target.value)}
          placeholder="请输入邮箱"
          className={errors.email ? 'border-red-500' : ''}
        />
        {errors.email && (
          <p className="text-sm text-red-500 mt-1">{errors.email}</p>
        )}
      </div>

      <div>
        <label htmlFor="username" className="block text-sm font-medium mb-1">
          用户名
        </label>
        <Input
          id="username"
          type="text"
          value={formData.username}
          onChange={(e) => handleChange('username', e.target.value)}
          placeholder="请输入用户名（2-50个字符）"
          className={errors.username ? 'border-red-500' : ''}
        />
        {errors.username && (
          <p className="text-sm text-red-500 mt-1">{errors.username}</p>
        )}
      </div>

      <div>
        <label htmlFor="password" className="block text-sm font-medium mb-1">
          密码
        </label>
        <Input
          id="password"
          type="password"
          value={formData.password}
          onChange={(e) => handleChange('password', e.target.value)}
          placeholder="请输入密码（至少6个字符）"
          className={errors.password ? 'border-red-500' : ''}
        />
        {errors.password && (
          <p className="text-sm text-red-500 mt-1">{errors.password}</p>
        )}
      </div>

      <div>
        <label htmlFor="confirmPassword" className="block text-sm font-medium mb-1">
          确认密码
        </label>
        <Input
          id="confirmPassword"
          type="password"
          value={formData.confirmPassword}
          onChange={(e) => handleChange('confirmPassword', e.target.value)}
          placeholder="请再次输入密码"
          className={errors.confirmPassword ? 'border-red-500' : ''}
        />
        {errors.confirmPassword && (
          <p className="text-sm text-red-500 mt-1">{errors.confirmPassword}</p>
        )}
      </div>

      <Button type="submit" className="w-full" disabled={isLoading}>
        {isLoading ? '注册中...' : '注册'}
      </Button>
    </form>
  );
}
```

### GitHubButton 组件

**文件**: `src/auth/components/GitHubButton.tsx`

```typescript
import { Button } from '@/components/ui/button';
import { loginWithGitHub } from '@/api/auth';
import { Github } from 'lucide-react';

export function GitHubButton() {
  const handleClick = () => {
    loginWithGitHub();
  };

  return (
    <Button
      type="button"
      variant="outline"
      className="w-full"
      onClick={handleClick}
    >
      <Github className="w-4 h-4" />
      使用 GitHub 登录
    </Button>
  );
}
```

### AuthLayout 组件

**文件**: `src/auth/components/AuthLayout.tsx`

```typescript
import { ReactNode } from 'react';
import { Link } from 'react-router-dom';

interface AuthLayoutProps {
  children: ReactNode;
  title: string;
  subtitle?: string;
  footerText?: string;
  footerLink?: string;
  footerLinkText?: string;
}

export function AuthLayout({
  children,
  title,
  subtitle,
  footerText,
  footerLink,
  footerLinkText,
}: AuthLayoutProps) {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50 dark:bg-gray-900 px-4">
      <div className="max-w-md w-full space-y-8">
        <div className="text-center">
          <h2 className="text-3xl font-bold">{title}</h2>
          {subtitle && (
            <p className="mt-2 text-sm text-gray-600 dark:text-gray-400">
              {subtitle}
            </p>
          )}
        </div>
        
        <div className="bg-white dark:bg-gray-800 p-8 rounded-lg shadow-md">
          {children}
        </div>

        {footerText && footerLink && footerLinkText && (
          <p className="text-center text-sm text-gray-600 dark:text-gray-400">
            {footerText}{' '}
            <Link
              to={footerLink}
              className="font-medium text-primary hover:underline"
            >
              {footerLinkText}
            </Link>
          </p>
        )}
      </div>
    </div>
  );
}
```

---

## 表单验证方案

### 验证工具函数

**文件**: `src/auth/utils/validation.ts`

```typescript
export interface ValidationErrors {
  [key: string]: string;
}

/**
 * 验证邮箱格式
 */
function isValidEmail(email: string): boolean {
  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return emailRegex.test(email);
}

/**
 * 验证登录表单
 */
export function validateLoginForm(data: {
  email: string;
  password: string;
}): ValidationErrors {
  const errors: ValidationErrors = {};

  if (!data.email) {
    errors.email = '请输入邮箱';
  } else if (!isValidEmail(data.email)) {
    errors.email = '邮箱格式不正确';
  }

  if (!data.password) {
    errors.password = '请输入密码';
  } else if (data.password.length < 6) {
    errors.password = '密码至少需要6个字符';
  }

  return errors;
}

/**
 * 验证注册表单
 */
export function validateRegisterForm(data: {
  email: string;
  username: string;
  password: string;
  confirmPassword?: string;
}): ValidationErrors {
  const errors: ValidationErrors = {};

  // 验证邮箱
  if (!data.email) {
    errors.email = '请输入邮箱';
  } else if (!isValidEmail(data.email)) {
    errors.email = '邮箱格式不正确';
  }

  // 验证用户名
  if (!data.username) {
    errors.username = '请输入用户名';
  } else if (data.username.length < 2) {
    errors.username = '用户名至少需要2个字符';
  } else if (data.username.length > 50) {
    errors.username = '用户名不能超过50个字符';
  }

  // 验证密码
  if (!data.password) {
    errors.password = '请输入密码';
  } else if (data.password.length < 6) {
    errors.password = '密码至少需要6个字符';
  } else if (data.password.length > 100) {
    errors.password = '密码不能超过100个字符';
  }

  // 验证确认密码
  if (data.confirmPassword !== undefined) {
    if (!data.confirmPassword) {
      errors.confirmPassword = '请确认密码';
    } else if (data.password !== data.confirmPassword) {
      errors.confirmPassword = '两次输入的密码不一致';
    }
  }

  return errors;
}
```

---

## Token 管理方案

### Token 工具函数

**文件**: `src/auth/utils/token.ts`

```typescript
const TOKEN_KEY = 'accessToken';
const USER_KEY = 'user';

/**
 * 保存 Token
 */
export function setToken(token: string): void {
  localStorage.setItem(TOKEN_KEY, token);
}

/**
 * 获取 Token
 */
export function getToken(): string | null {
  return localStorage.getItem(TOKEN_KEY);
}

/**
 * 移除 Token
 */
export function removeToken(): void {
  localStorage.removeItem(TOKEN_KEY);
}

/**
 * 保存用户信息
 */
export function setUser(user: any): void {
  localStorage.setItem(USER_KEY, JSON.stringify(user));
}

/**
 * 获取用户信息
 */
export function getUser(): any | null {
  const userStr = localStorage.getItem(USER_KEY);
  if (!userStr) return null;
  try {
    return JSON.parse(userStr);
  } catch {
    return null;
  }
}

/**
 * 移除用户信息
 */
export function removeUser(): void {
  localStorage.removeItem(USER_KEY);
}

/**
 * 清除所有认证信息
 */
export function clearAuth(): void {
  removeToken();
  removeUser();
}
```

---

## 错误处理方案

### 错误处理工具

**文件**: `src/utils/error.ts`

```typescript
import { AxiosError } from 'axios';
import { toast } from 'sonner';

export interface ApiError {
  statusCode: number;
  message: string | string[];
  error: string;
}

/**
 * 处理 API 错误
 */
export function handleApiError(error: unknown): string {
  if (error instanceof AxiosError) {
    const apiError = error.response?.data as ApiError;
    
    if (apiError?.message) {
      if (Array.isArray(apiError.message)) {
        return apiError.message[0];
      }
      return apiError.message;
    }
    
    if (error.response?.status === 401) {
      return '未授权，请重新登录';
    }
    
    if (error.response?.status === 403) {
      return '没有权限访问';
    }
    
    if (error.response?.status === 404) {
      return '资源不存在';
    }
    
    if (error.response?.status === 500) {
      return '服务器错误，请稍后重试';
    }
    
    if (error.code === 'ECONNABORTED') {
      return '请求超时，请稍后重试';
    }
    
    if (error.code === 'ERR_NETWORK') {
      return '网络错误，请检查网络连接';
    }
  }
  
  return '发生未知错误，请稍后重试';
}

/**
 * 显示错误提示
 */
export function showError(error: unknown): void {
  const message = handleApiError(error);
  toast.error(message);
}
```

---

## 保护路由方案

### 已在前面的路由设计部分说明

---

## 详细实现步骤

### 步骤 1: 创建目录结构

```bash
# 在 client/src 目录下创建以下目录
mkdir -p src/api
mkdir -p src/auth/components
mkdir -p src/auth/hooks
mkdir -p src/auth/utils
mkdir -p src/context
mkdir -p src/pages
mkdir -p src/types
```

### 步骤 2: 创建类型定义

1. 创建 `src/types/auth.ts`（内容见类型定义部分）
2. 创建 `src/api/types.ts`（内容见 API 类型定义部分）

### 步骤 3: 创建 Token 管理工具

1. 创建 `src/auth/utils/token.ts`（内容见 Token 管理方案部分）

### 步骤 4: 创建表单验证工具

1. 创建 `src/auth/utils/validation.ts`（内容见表单验证方案部分）

### 步骤 5: 创建错误处理工具

1. 创建 `src/utils/error.ts`（内容见错误处理方案部分）

### 步骤 6: 配置 Axios

1. 创建 `src/api/axios.ts`（内容见 API 服务层设计部分）

### 步骤 7: 创建认证 API

1. 创建 `src/api/auth.ts`（内容见 API 服务层设计部分）

### 步骤 8: 创建 AuthContext

1. 创建 `src/context/AuthContext.tsx`（内容见状态管理方案部分）

### 步骤 9: 创建 AuthProvider

1. 创建 `src/context/AuthProvider.tsx`（内容见状态管理方案部分）
2. 更新 `src/context/AuthProvider.tsx`，实现 `login` 和 `register` 方法：

```typescript
const login = async (email: string, password: string) => {
  try {
    const response = await loginApi({ email, password });
    setTokenState(response.accessToken);
    setUserState(response.user);
    toast.success('登录成功');
  } catch (error) {
    showError(error);
    throw error;
  }
};

const register = async (email: string, username: string, password: string) => {
  try {
    const response = await registerApi({ email, username, password });
    setTokenState(response.accessToken);
    setUserState(response.user);
    toast.success('注册成功');
  } catch (error) {
    showError(error);
    throw error;
  }
};
```

### 步骤 10: 创建 useAuth Hook

1. 创建 `src/auth/hooks/useAuth.ts`（内容见状态管理方案部分）

### 步骤 11: 创建认证组件

1. 创建 `src/auth/components/LoginForm.tsx`（内容见组件设计部分）
2. 创建 `src/auth/components/RegisterForm.tsx`（内容见组件设计部分）
3. 创建 `src/auth/components/GitHubButton.tsx`（内容见组件设计部分）
4. 创建 `src/auth/components/AuthLayout.tsx`（内容见组件设计部分）

### 步骤 12: 创建页面组件

1. 创建 `src/pages/Login.tsx`:

```typescript
import { LoginForm } from '@/auth/components/LoginForm';
import { GitHubButton } from '@/auth/components/GitHubButton';
import { AuthLayout } from '@/auth/components/AuthLayout';
import { Link } from 'react-router-dom';

export default function Login() {
  return (
    <AuthLayout
      title="登录"
      subtitle="欢迎回来"
      footerText="还没有账号？"
      footerLink="/register"
      footerLinkText="立即注册"
    >
      <div className="space-y-6">
        <LoginForm />
        <div className="relative">
          <div className="absolute inset-0 flex items-center">
            <div className="w-full border-t border-gray-300"></div>
          </div>
          <div className="relative flex justify-center text-sm">
            <span className="px-2 bg-white dark:bg-gray-800 text-gray-500">
              或
            </span>
          </div>
        </div>
        <GitHubButton />
      </div>
    </AuthLayout>
  );
}
```

2. 创建 `src/pages/Register.tsx`:

```typescript
import { RegisterForm } from '@/auth/components/RegisterForm';
import { GitHubButton } from '@/auth/components/GitHubButton';
import { AuthLayout } from '@/auth/components/AuthLayout';
import { Link } from 'react-router-dom';

export default function Register() {
  return (
    <AuthLayout
      title="注册"
      subtitle="创建新账号"
      footerText="已有账号？"
      footerLink="/login"
      footerLinkText="立即登录"
    >
      <div className="space-y-6">
        <RegisterForm />
        <div className="relative">
          <div className="absolute inset-0 flex items-center">
            <div className="w-full border-t border-gray-300"></div>
          </div>
          <div className="relative flex justify-center text-sm">
            <span className="px-2 bg-white dark:bg-gray-800 text-gray-500">
              或
            </span>
          </div>
        </div>
        <GitHubButton />
      </div>
    </AuthLayout>
  );
}
```

3. 创建 `src/pages/AuthCallback.tsx`:

```typescript
import { useEffect } from 'react';
import { useNavigate, useSearchParams } from 'react-router-dom';
import { useAuth } from '@/auth/hooks/useAuth';
import { setToken, setUser } from '@/auth/utils/token';
import { toast } from 'sonner';

export default function AuthCallback() {
  const navigate = useNavigate();
  const [searchParams] = useSearchParams();
  const { setToken: setTokenState, setUser: setUserState } = useAuth();

  useEffect(() => {
    const token = searchParams.get('token');
    
    if (token) {
      // 保存 Token
      setToken(token);
      setTokenState(token);
      
      // 注意：GitHub 登录回调时，后端只返回 token，不返回用户信息
      // 需要调用获取用户信息的接口（如果有的话）
      // 或者从 token 中解析用户信息（如果 token 包含）
      
      toast.success('登录成功');
      navigate('/', { replace: true });
    } else {
      toast.error('登录失败');
      navigate('/login', { replace: true });
    }
  }, [searchParams, navigate, setTokenState, setUserState]);

  return (
    <div className="flex items-center justify-center min-h-screen">
      <div className="text-lg">正在登录...</div>
    </div>
  );
}
```

### 步骤 13: 创建路由保护组件

1. 创建 `src/components/ProtectedRoute.tsx`（内容见路由设计部分）
2. 创建 `src/components/PublicRoute.tsx`（内容见路由设计部分）

### 步骤 14: 更新 App.tsx

1. 更新 `src/App.tsx`，添加 AuthProvider 和新的路由（内容见路由设计部分）

### 步骤 15: 环境变量配置

1. 创建或更新 `.env` 文件：

```env
VITE_API_BASE_URL=http://localhost:3000
```

### 步骤 16: 测试

1. 启动后端服务
2. 启动前端服务
3. 测试注册功能
4. 测试登录功能
5. 测试 GitHub 登录功能
6. 测试路由保护功能

---

## 测试方案

### 功能测试清单

- [ ] 注册功能
  - [ ] 正常注册
  - [ ] 邮箱格式验证
  - [ ] 用户名长度验证
  - [ ] 密码长度验证
  - [ ] 确认密码匹配验证
  - [ ] 邮箱已存在错误处理
  
- [ ] 登录功能
  - [ ] 正常登录
  - [ ] 邮箱格式验证
  - [ ] 密码错误处理
  - [ ] 邮箱不存在处理
  
- [ ] GitHub 登录
  - [ ] 跳转到 GitHub
  - [ ] 授权后回调
  - [ ] Token 保存
  - [ ] 自动注册/登录
  
- [ ] 路由保护
  - [ ] 未登录访问受保护路由
  - [ ] 已登录访问公开路由
  - [ ] Token 过期处理
  
- [ ] Token 管理
  - [ ] Token 保存
  - [ ] Token 读取
  - [ ] Token 清除
  - [ ] 自动添加 Token 到请求头

---

## 注意事项

1. **环境变量**: 确保配置正确的 API Base URL
2. **CORS**: 确保后端已配置允许前端域名
3. **Token 安全**: 生产环境考虑使用 HttpOnly Cookie
4. **错误处理**: 统一使用 toast 显示错误信息
5. **加载状态**: 所有异步操作都要显示加载状态
6. **表单验证**: 前端验证 + 后端验证双重保障
7. **用户体验**: 友好的错误提示和成功提示

---

## 总结

本方案提供了完整的前端登录注册实现方案，包括：
- ✅ 完整的项目结构
- ✅ 状态管理方案
- ✅ 路由保护方案
- ✅ API 服务层设计
- ✅ 组件设计
- ✅ 表单验证
- ✅ 错误处理
- ✅ 详细实现步骤

按照本方案逐步实现，可以构建一个规范、可维护的认证系统。

---

**文档版本**: v1.0  
**最后更新**: 2024

